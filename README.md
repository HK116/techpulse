# TechPulse

A small data pipeline that fetches trending Hacker News stories, stores them, and uses an LLM (e.g., Claude) to summarize
and categorize them - served through a FastAPI service.

> Under active development. See commit history for build progress